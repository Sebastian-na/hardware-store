import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#1C1C1E',
    },
    secondary: {
      main: '#cba81a',
    },
  },
})

export default theme
